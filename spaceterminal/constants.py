class URL:
    """URL endpoints for SpaceTraders API."""

    BASE = "https://api.spacetraders.io/v2"
    REGISTER = f"{BASE}/register"
    AGENT = f"{BASE}/my/agent"
    FACTIONS = f"{BASE}/factions"
    SHIPS = f"{BASE}/my/ships"
    CONTRACTS = f"{BASE}/my/contracts"
